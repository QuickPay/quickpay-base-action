{
  "name": "quickpay-base-actions",
  "version": "1.0.0",
  "description": "sets up the base envoriment that most quickpay actions require",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "ncc build index.js -o dist --source-map && cp action.yml ./dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/QuickPay/quickpay-base-action"
  },
  "keywords": [
    "GitHub",
    "Actions"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.5.0",
    "@actions/exec": "^1.1.0",
    "@actions/github": "^5.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.0",
    "eslint": "^7.32.0"
  }
}